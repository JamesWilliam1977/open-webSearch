interface:
  display_name: "Open WebSearch"
  short_description: "Repo-specific patterns and workflows for open-webSearch"
  default_prompt: "Use the open-webSearch repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true